node_modules/
.wrangler/
.dev.vars
.DS_Store
.claude/worktrees/

# Built client bundle — regenerated by `npm run build` (which `deploy` and
# `dev` run first), so it is not committed.
public/app.js
public/app.js.map
